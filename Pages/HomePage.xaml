<Page x:Class="AuroraStarLauncher.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      xmlns:iconPacks = "http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:AuroraStarLauncher.Pages"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
      d:DesignHeight="440" d:DesignWidth="790"
      Title="HomePage">
    <Grid>
        <Button 
             HorizontalAlignment="Right" 
             VerticalAlignment="Bottom"
             Margin="0,0,20,20"
            Background="Transparent" Click="Button_Click"
            >
            <StackPanel Orientation="Horizontal">
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock FontSize="20" Text="请选择版本" HorizontalAlignment="Right" x:Name="Show_Game_Ver"/>
                    <TextBlock FontSize="10" Text="启动游戏" HorizontalAlignment="Right"/>
                </StackPanel>
                <TextBlock FontSize="30" Text=">"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>
